/*
 * This file was generated by the Gradle 'init' task.
 */

plugins {
    id 'application'
    id 'java'
    id 'net.ltgt.errorprone' version "4.0.1"
}


repositories {
    mavenCentral()
    gradlePluginPortal()
}

dependencies {
    implementation("com.github.freva:ascii-table:1.9.0")
    testImplementation("org.junit.jupiter:junit-jupiter-api:5.14.3")
    testImplementation("org.junit.jupiter:junit-jupiter-engine:5.14.3")
    testImplementation("org.junit.platform:junit-platform-launcher:1.10.0")
    errorprone 'com.google.errorprone:error_prone_core:2.28.0'
}

application {
    mainClass = 'edu.badpals.brunosbox.Brunosbox'
}

jar {
    manifest {
        attributes 'Main-Class' : 'edu.badpals.brunosbox.Brunosbox',
                   'Package-Title' : 'Brunosbox',
                   'Package-Version': '1.0',
                   'Package-Vendor' : 'davidg@edu.xunta.gal',
                   'Specification-Title' : 'Examen programacion'
    }
}

// construir con: gradle fatJar
// java -jar build/libs/brunosbox-1.0-SNAPSHOT.jar
task fatJar(type: Jar) {
    manifest {
        attributes 'Main-Class' : 'edu.badpals.brunosbox.Brunosbox',
                   'Package-Title' : 'Brunosbox',
                   'Package-Version': '1.0',
                   'Package-Vendor' : 'davidg@edu.xunta.gal',
                   'Specification-Title' : 'Examen programacion'
    }
    duplicatesStrategy = DuplicatesStrategy.INCLUDE
    from { configurations.compileClasspath.collect { it.isDirectory() ? it : zipTree(it) } }
    with jar
}

group = 'edu.badpals'
version = '1.0-SNAPSHOT'
description = 'brunosbox'
java.sourceCompatibility = JavaVersion.VERSION_21


tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

tasks.withType(Javadoc) {
    options.encoding = 'UTF-8'
}

test {
    useJUnitPlatform()
}
